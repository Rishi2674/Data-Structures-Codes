#include <stdio.h>
#include <stdlib.h>

int main(){
    int *arr[100];
    FILE *f = fopen("input.txt","r");
    for(int i=0;i<100;i++){
        arr[i] = (int *)malloc(1000*size(int));
        
    }
}